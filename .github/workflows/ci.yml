name: Resume ci Bulid

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image with tag
      run: docker build . --file Dockerfile --tag my-image-name:${{github.run_number}}
    - name: Build latest
      run: docker build . --file Dockerfile --tag my-image-name
    - name: Login to Docker Hub securely
      run: echo "${{62010786Docker! }}" | docker login -u "Kirubel96" --password-stdin
    - name: Tag image
      run: docker tag my-image-name:${{github.run_number}} ${{Kirubel96}}/my-image-name:${{github.run_number}}
    - name: Push image to docker
      run: docker push ${{ Kirubel96 }}/my-image-name:${{ github.run_number}} 